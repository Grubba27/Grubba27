name: Generate Snake

on:
  schedule: 
    - cron: "* */12 * * *"
  workflow_dispatch:

jobs:
  build:
    name: Jobs to update snake
    runs-on: ubuntu-latest
    steps:
      # Snake Animation
      - uses: Platane/snk@master
        id: snake-gif
        with:
          github_user_name: Grubba27
          svg_out_path: ./github-contribution-grid-snake.svg
